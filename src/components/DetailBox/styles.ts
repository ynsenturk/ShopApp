import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  viewDetailContainer: {
    width: "100%",
    alignItems: "flex-start",
  },
  textName: { fontWeight: "700", fontSize: 20, marginTop: 10 },
  textDescription: {
    fontWeight: "400",
    fontSize: 14,
    marginTop: 15,
  },
});

export default styles;
